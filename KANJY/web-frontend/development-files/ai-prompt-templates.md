# KANJY AI プロンプトテンプレート集

## 🎯 基本テンプレート

### フルスペック版（初回推奨）

```
KANJYプロジェクトのデザインシステムに厳密に従ってコードを作成してください。

【デザインシステム】
カラーパレット:
- プライマリー: #007AFF (iOS Blue)
- 成功: #28a745 (Green)  
- 警告: #ffc107 (Yellow)
- エラー: #dc3545 (Red)
- 背景: #f5f5f5 (Light Gray)
- カード背景: #f8f9fa
- テキスト: #333333 (Dark), #6c757d (Medium)
- ボーダー: #e9ecef

フォントシステム:
- font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif
- フォントサイズ: 12px, 14px, 16px, 18px, 20px, 24px
- フォントウェイト: 300, 400, 500, 600, 700

スペーシング (8pt Grid):
- XS: 4px, SM: 8px, MD: 16px, LG: 24px, XL: 32px

エフェクト:
- 角丸: 4px (小), 8px (標準), 12px (大)
- シャドウ: 0 4px 6px rgba(0, 0, 0, 0.1)
- アニメーション: transition: all 0.2s ease

【スタイル原則】
- iOS風クリーンデザイン
- レスポンシブ対応必須
- CSS変数の活用
- アクセシビリティ配慮

【要件】
[ここに具体的な機能要件を記載]

【出力形式】
HTML + CSS (バニラJavaScript)
```

### コンパクト版（継続使用時）

```
KANJY デザインシステム準拠でコード作成：

カラー: #007AFF(プライマリー), #28a745(成功), #dc3545(エラー)
フォント: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto
スペーシング: 8px, 16px, 24px (8pt grid)
角丸: 8px, シャドウ: 0 4px 6px rgba(0,0,0,0.1)
アニメーション: transition: all 0.2s ease

要件: [具体的な機能]
出力: HTML + CSS (バニラJS)
```

## 🧩 機能別テンプレート

### ボタンコンポーネント

```
KANJYデザインシステム準拠のボタンを作成：

スタイル要件:
- 背景: #007AFF (プライマリー), #28a745 (成功), #dc3545 (エラー)
- パディング: 8px 16px
- 角丸: 8px
- フォント: -apple-system, weight: 600
- ホバー: 少し暗く + シャドウ追加
- transition: all 0.2s ease

バリエーション: プライマリー、成功、エラー、セカンダリー
```

### カードコンポーネント

```
KANJYデザインシステム準拠のカードを作成：

スタイル要件:
- 背景: #ffffff
- パディング: 24px
- 角丸: 12px
- シャドウ: 0 4px 6px rgba(0, 0, 0, 0.1)
- ボーダー: 1px solid #e9ecef
- iOS風のクリーンなデザイン
```

### フォーム要素

```
KANJYデザインシステム準拠のフォーム要素を作成：

スタイル要件:
- 入力フィールド: パディング 8px 16px, ボーダー 2px solid #e9ecef
- フォーカス: ボーダー #007AFF, シャドウ 0 0 0 3px rgba(0,122,255,0.1)
- ラベル: フォントウェイト 600, カラー #333333
- エラー状態: ボーダー #dc3545
```

## 📱 レスポンシブ対応

```
KANJYレスポンシブデザイン準拠：

ブレイクポイント:
- SM: 640px
- MD: 768px  
- LG: 1024px
- XL: 1280px

モバイル優先設計
タッチフレンドリー（最小44px）
```

## 🎨 特殊用途テンプレート

### 統計・グラフ表示

```
KANJYデザインシステム準拠の統計表示：

カラー:
- 参加: #28a745
- 不参加: #dc3545  
- 未定: #ffc107

スタイル: カード形式、数値強調、アイコン使用
アニメーション: フェードイン、プログレスバー
```

### 通知・アラート

```
KANJYデザインシステム準拠の通知：

タイプ別カラー:
- 成功: #28a745背景
- 警告: #ffc107背景
- エラー: #dc3545背景
- 情報: #007AFF背景

スタイル: 角丸8px、アイコン付き、自動消滅
```

## 🔧 デバッグ・修正用

### スタイル修正

```
以下のコードをKANJYデザインシステムに準拠するよう修正：

[既存コード]

修正点:
- ハードコードされた色をKANJYカラーパレットに変更
- スペーシングを8pt gridに統一
- フォントをApple系に変更
- iOS風の角丸・シャドウに調整
```

## 💡 使い方のコツ

1. **初回**: フルスペック版でAIに学習させる
2. **継続**: コンパクト版で効率化
3. **機能別**: 専用テンプレートで精度向上
4. **修正**: デバッグ用で既存コード改善 